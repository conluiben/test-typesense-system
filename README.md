# Test Typesense System

Requirements: Node.js, Docker

Steps to install:
1. Clone repository
2. Traverse to project repository:
```
cd /test-typesense-system
```
4. Run project with:
```
docker compose up -d --build
```
4. Access the frontend at http://localhost:3000.
